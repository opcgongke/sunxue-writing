# Contributing Guide

欢迎补充和改进这个 skill。请按以下方式参与：

- 提交前先阅读 `SKILL.md` 的规则结构。
- 如果调整规则，请同时更新 `manifest.json` 与 `skillhub.json` 的版本号。
- 涉及结构模板（叙事骨架）时，请同步更新示例与说明，避免模式漂移。
- 保持中文写作风格规则向下兼容，避免加入强制性的输出格式限制。

建议的提交内容：

1. 改进 Prompt 模板
2. 增加更多示例（尤其是回环结构示例）
3. 补充失败案例和自检清单
